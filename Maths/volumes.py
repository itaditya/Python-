import math

def volume_cube(side):
  return side**3
  
def volume_rectangular(length, width, height):
  return length * width * height
  
def volume_cylinder(base, height):
  return base * height
  
def volume_cone(base, height)
  return (1/3) * base * height
  
def volume_pyramid(base, height):
  return (1/3) * base * height
  
def volume_sphere(ray):
  return (4/3) * math.pi * (ray**3)
